function divisibleByThree(num, div) {
return ((num.split('').reduce((sum, current) => +sum + +current)/div 
- Math.floor(num.split('').reduce((sum, current) => +sum + +current)/div)) == 0); }
alert(divisibleByThree('1234',3));
