"use strict";
function divisibleByThree(num) {
let arr = num.split('').reduce((sum, current) => +sum + +current);
let div = 3;
let dif = arr/div - Math.floor(arr/div);
if (dif == 0) { return true; }
else { return false };
}
alert(divisibleByThree('1236'));
